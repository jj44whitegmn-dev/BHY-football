发这个给Claude Code：

请检查并修复以下两个问题：
问题一：S2和S3的AI识别状态不透明
当前S2/S3显示0时，用户无法判断是"AI判断为中性"还是"AI识别失败默认返回0"。
请修改为三态显示：
AI已分析且判断为中性 → 显示 0，角标"AI✓"
AI识别失败或未上传截图 → 显示 ?，角标"待输入"，按钮高亮提示手动选择
用户手动覆盖 → 显示分值，角标"手动"
问题二：请实际测试当前AI视觉函数能否正确识别S2和S3
用以下方式验证：
第一步，检查recognizeOpening和recognizeWilliamTimeline的提示词，确认是否包含以下提取指令：
S2所需数据：
平博初盘：open_home、open_away（最底部一行）
威廉初盘：wh_open_home、wh_open_away（最底部一行）
S2判断逻辑：
  平博初盘和威廉初盘方向相反且差值>0.10 → 跟平博方向
  方向一致或差值≤0.10 → 0
S3所需数据：
平博终盘：close_home、close_away（recognizeClosing最顶部一行）
S3判断逻辑：
  主水比客水低超过0.08 → +1
  客水比主水低超过0.08 → -1
  差值≤0.08 → 0
第二步，用下面这组测试数据手动调用AI接口，看返回值是否正确：
平博初盘：主1.89 / 客1.93（平手）→ 平博初盘轻微偏客
威廉初盘：主1.85 / 客1.89（平手）→ 威廉初盘轻微偏客
预期S2：两家方向一致，差值均<0.10 → S2应返回0

平博终盘：主1.83 / 客1.92
预期S3：客水比主水低0.09，超过0.08 → S3应返回-1（偏客）
第三步，输出实际测试结果：
S2识别测试：返回值X，预期0，是否正确？
S3识别测试：返回值X，预期-1，是否正确？
识别失败原因（如有）：
问题三：如果发现AI无法正确识别S2或S3，按以下优先级修复
优先级1：修复提示词，确保AI返回正确字段和判断
优先级2：如果提示词已正确但AI仍返回错误值
          在UI上将该信号标记为"建议手动确认"
          不强制依赖AI输出
优先级3：确保recognizeWilliamTimeline的返回值
          正确传入autoCalc的S2计算逻辑
          检查字段名是否匹配（wh_open_home vs willOpenHome等）
完成测试和修复后，输出：
S2当前状态：AI可识别 / AI不可识别（原因）/ 已修复
S3当前状态：AI可识别 / AI不可识别（原因）/ 已修复
三态显示：已实现