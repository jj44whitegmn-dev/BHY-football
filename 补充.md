这版 v3.1 已经可以开工，但请在写代码前再补以下 6 个修正：

1. 明确 ctf_eu_diff 的符号含义，不要再写“正值=体彩更甜”这种容易误导的注释。请明确：
   - ctf_eu_diff = ctf_nv - eu_close_nv
   - 负值通常意味着体彩该方向赔率相对更高、相对更甜
   并新增更直观的字段：
   - value_hint_side
   - value_hint_strength

2. 亚盘方向判断不要只用 delta 生硬判断，请封装函数来解释：
   - 开盘谁是热门
   - 终盘谁是热门
   - 热门强度是增强还是减弱
   以避免平手盘、受让盘、跨零盘误判。

3. 请增加 risk_score 和明确的 risk_level 生成规则，不要让 risk_level 成为拍脑袋字段。

4. 请增加 quality_breakdown 字段，用于记录质量分扣减来源；并对 quality_score 做 0-100 钳制。

5. 请把 suggested_side 和 whether_to_bet 明确区分：
   - 可以有方向倾向，但仍然不建议下注
   - 不要把“推荐方向”和“是否建议下注”完全绑定

6. 请调整模块职责：
   - storage.js 只负责本地存储、迁移、CRUD
   - engine.js 只负责分析
   - ui.js 负责调度 storage + engine
   不要让 storage.js 依赖 engine.js

请先更新最终设计说明，再开始按文件顺序写代码。


